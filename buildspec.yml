version: 0.2

phases:
  install:
    commands:
      # Upgrade AWS CLI to the latest version
      - pip install --upgrade awscli
  pre_build:
    commands:
      - mvn clean compile test
  build:
    commands:
      - mvn package
artifacts:
  type: zip
  files:
    - 'target/user-portal-0.0.1-SNAPSHOT.jar'
